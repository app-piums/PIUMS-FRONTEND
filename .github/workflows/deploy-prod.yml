# This workflow has been superseded by backend-deploy-prod.yml which handles
# both backend services and frontend apps in a single coordinated blue-green deploy.
# Keeping this file disabled to avoid double-deploys on release events.
name: Deploy to Production (DISABLED)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'This workflow is disabled. Use backend-deploy-prod.yml instead.'
        required: true

jobs:
  noop:
    name: Disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use backend-deploy-prod.yml for production deployments."
